include required(classpath("application"))

# Cromwell 92 has a hardcoded allowlist of registries for Docker hash lookup
# (used for call caching) and does not include ghcr.io. Disabling the lookup
# lets tasks that reference ghcr.io images run; call caching is not used in CI.
docker {
  hash-lookup {
    enabled = false
  }
}
